@model SignalRChat.Models.BasicCookie

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>HSW site</title>
</head>
<body>

<h4>Найди себе друга ^^</h4>
<table class="table">
    <tr>
        <th>
            Nickname
        </th>
        <th>
            Возраст
        </th>
        <th>
            Последний визит
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.GetUserList())
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nickname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastVisit)
            </td>
            <td>
                @Html.ActionLink("Добавить в друзья", "AddToFriends", new {id = item.Id, userId = Model.GetUser().Id}) |
                @Html.ActionLink("Чат", "RedirectToCreateChat", new { id = item.Id, userId = Model.GetUser().Id})
            </td>
        </tr>
    }

</table>
</body>
</html>
